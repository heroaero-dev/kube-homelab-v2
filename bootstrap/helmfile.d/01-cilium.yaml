helmDefaults:
  wait: true
  timeout: 600

repositories:
  - name: cilium
    url: https://helm.cilium.io

releases:
  - name: cilium
    namespace: kube-system
    chart: cilium/cilium
    version: 1.16.3
    values:
      - templates/values-cilium.yaml.gotmpl

