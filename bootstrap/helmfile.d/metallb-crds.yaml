helmDefaults:
  wait: true
  timeout: 300

repositories:
  - name: metallb
    url: https://metallb.github.io/metallb

releases:
  - name: metallb
    namespace: metallb-system
    chart: metallb/metallb
    version: 0.15.3
    installed: true
    values:
      - crds:
          enabled: true

