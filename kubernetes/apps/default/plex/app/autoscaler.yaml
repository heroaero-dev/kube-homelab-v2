apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: autoscaler
spec:
  scaleTargetRef:
    name: plex
    apiVersion: apps/v1
    kind: Deployment
  minReplicaCount: 1
  maxReplicaCount: 5
  cooldownPeriod: 60   # how long until scale down
  triggers:
    - type: cpu
      metadata:
        metricType: Utilization
        value: "75"      # scale if CPU > 75%
    - type: memory
      metadata:
        metricType: Utilization
        value: "80"      # scale if RAM > 80%

