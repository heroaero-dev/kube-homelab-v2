apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  namespace: default
  labels:
    app: jellyfin
spec:
  type: ClusterIP
  selector:
    app: jellyfin
  ports:
    - name: http
      port: 8096
      targetPort: http

