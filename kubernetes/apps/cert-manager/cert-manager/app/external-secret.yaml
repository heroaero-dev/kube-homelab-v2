apiVersion: external-secrets.io/v1
kind: ExternalSecret 
metadata:
  name: cloudflare-dns-token
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore 
  target:
    name: cloudflare-dns-token
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: cloudflare-issuer-secret
